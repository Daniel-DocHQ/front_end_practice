import React, { memo } from 'react';
import PickupManagement from '../../components/SAComponents/PickupManagement';

const SAPickupManagement = () => (
    <PickupManagement />
);

export default memo(SAPickupManagement);
